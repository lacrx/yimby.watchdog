# Anthropic API key — only needed for 'full' mode bulk processing.
# The nightly pipeline uses claude -p (subscription) and strips this from the environment.
# ANTHROPIC_API_KEY=sk-ant-...

# S3 bucket for archival backup (optional — pipeline works fine without it)
# S3_BUCKET=civics-monitor-ACCOUNT_ID

# Discord webhook for new meeting notifications (optional)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
